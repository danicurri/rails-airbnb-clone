<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3 col-md-offset-4 col-md-4 text-center">
      <h1>Host a new flat</h1>
      <%= simple_form_for @flat do |f| %>
      <%= f.input :title, placeholder: 'Modernista Two Bed' %>
      <%= f.input :address, placeholder: 'Street, City'  %>
      <%= f.input :description, placeholder: '2 bed family home'  %>
      <%= f.input :price, placeholder: '120€'  %>
      <%= f.input :photo %>
      <%= f.input :photo_cache, as: :hidden %>
      <%= f.submit "add a flat", class: "btn-submit" %>
      <% end %>
    </div>
  </div>
</div>
